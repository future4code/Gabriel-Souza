import styled from "styled-components";

export const Container = styled.footer`
  text-align: center;
  height: 20px;

  background-color: #FFFF;

  font-size: clamp(0.500rem, 0.700vw, 0.900rem);
  font-family: var( --font-montserrat);
  font-weight: 500;

  display: flex;
  justify-content: center;
  align-items: center;
`;